# cimd-proxy
An OAuth 2.1 authorization server that speaks Client ID Metadata Documents outward and federates to an existing OIDC provider (e.g. Keycloak) inward, so MCP clients connect by URL alone. It never issues, verifies, parses or signs a token.
